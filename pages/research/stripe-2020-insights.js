import Index, {
  meta,
  title,
} from '../../articles/stripe-2020-insights/index.md'

import { Article } from '@carbonplan/layouts'

const Content = () => (
  <Article references={{}} meta={meta} title={title}>
    <Index />
  </Article>
)

export default Content
