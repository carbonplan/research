import Index, { meta } from '../../articles/natural-working-lands/index.md'

import { Article } from '@carbonplan/layouts'

const Content = () => (
  <Article references={{}} meta={meta} displayTitle={null}>
    <Index />
  </Article>
)

export default Content
